# Ensure backend (and other packages) can resolve hoisted deps when root
# node_modules is shared. Prefer explicit linking; shamefully-hoist flattens
# so symlinks resolve correctly from any workspace package.
shamefully-hoist=true
