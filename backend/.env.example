# Server
PORT=3001

# Database (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@localhost:5432/academy
CORS_ORIGIN=http://localhost:3000
AUTH_DOMAIN=localhost
AUTH_URI=http://localhost:3000
AUTH_CHAIN_ID=solana:devnet
PISTON_EXECUTE_URL=https://emkc.org/api/v2/piston/execute

# Solana
RPC_URL=https://api.devnet.solana.com
# Optional, derived from RPC_URL if omitted
# RPC_WS_URL=wss://api.devnet.solana.com
PROGRAM_ID=ACADBRCB3zGvo1KSCbkztS33ZNzeBv2d7bqGceti3ucf

# Base64-encoded secret key (Uint8Array JSON encoded then base64)
BACKEND_SIGNER_KEYPAIR=

# Required for admin scripts like `pnpm -C backend create-course`
AUTHORITY_KEYPAIR=
